#include "lists.h"

/**
* _strlen - check the code
* @s: pointer
* Return: strlength
*/

int _strlen(char *b)
{
	int strlength;

	for (strlength = 0; b[strlength] != '\0'; strlength++)
	{
		continue;
	}
	return (strlength);
}
